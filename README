
This project takes a config file and converts and entire PHP site into static html and uploads it to Amazon S3.

Usage:

php -f example_bake.php

or

php -f bake.php www.example.com.php


You could probably adapt this to use and static host.