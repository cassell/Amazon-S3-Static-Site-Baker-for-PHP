
This project takes a config file and converts and entire PHP site into static html and uploads it to Amazon S3 for hosting.

Usage:

php -f example_bake.php

or

php -f bake.php www.example.com.php
(if you have multiple sites you want to bake)